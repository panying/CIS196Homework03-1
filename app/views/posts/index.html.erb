<h1>Guestbook</h1>
 
<table>
  <tr>
    <th>Subject</th>
    <th>What they said...</th>
  </tr>
 
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.text %></td>
    </tr>
  <% end %>
</table>
<%= link_to 'I want to tell you something interesting!', :action => :new %>